---
applyTo: "backend/**/*,*.py"
---

## 後端指南

- 所有 API 端點必須定義在 `routers` 資料夾中。
- 從 `database.py` 檔案載入範例資料庫內容。
- 錯誤處理僅在伺服器端記錄。不要傳播到前端。
- 確保所有 API 都在文件中說明。
- 驗證後端的變更是否反映在前端 (`src/static/**`)。如果發現可能的破壞性變更,請向開發人員提及。